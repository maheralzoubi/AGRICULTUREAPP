import 'package:flutter/material.dart';

class DetailsAtempChart extends StatelessWidget {
  const DetailsAtempChart({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: Container(),
    );
  }
}
